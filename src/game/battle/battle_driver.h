#pragma once
/* battle_driver.h — Skeleton battle loop driver.
 *
 * Battle_RunLoop: blocking turn loop for smoke-testing the battle engine.
 * Auto-selects the player's first available move each turn.
 * Prints turn-by-turn state to stdout.
 * Returns when the battle ends (faint or escape).
 *
 * Replace with real UI-driven loop once battle rendering is implemented.
 */
void Battle_RunLoop(void);
